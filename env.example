############################################
# Biometric Core Backend - Local Dev Config #
############################################

# Runtime
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Postgres (docker-compose maps host 5433 -> container 5432)
# User/pass/db must match docker-compose.yml
DATABASE_URL=postgresql://postgres:asalbaca@localhost:5433/biometric_core?schema=public

# Redis (docker-compose maps host 6380 -> container 6379)
REDIS_URL=redis://localhost:6380

# JWT secrets (change in any shared environment)
AUTH_JWT_ACCESS_SECRET=dev-access-secret-change-me
AUTH_JWT_REFRESH_SECRET=dev-refresh-secret-change-me

# Optional TTL overrides (defaults exist in code)
# AUTH_JWT_ACCESS_TTL=900
# AUTH_JWT_REFRESH_TTL=604800
# STEP_UP_TOKEN_TTL_SECONDS=120

# WebAuthn / Biometric configuration
# Required when NODE_ENV is not "test"
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGINS=http://localhost:3000
# WEBAUTHN_RP_NAME=Biometric Core (Local)
# WEBAUTHN_CHALLENGE_TTL_MS=180000
# WEBAUTHN_SIGNCOUNT_MODE=strict

# Optional email delivery (leave unset to use MockEmailService)
# RESEND_API_KEY=
# EMAIL_FROM_ADDRESS=
# EMAIL_FROM_NAME=
# EMAIL_VERIFICATION_URL=
# PASSWORD_RESET_URL=


