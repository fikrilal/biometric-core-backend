NODE_ENV=development
PORT=3000

# Postgres connection string
DATABASE_URL=postgresql://postgres:asalbaca@localhost:5433/biometric_core?schema=public

# Redis connection string
REDIS_URL=redis://localhost:6380

# Logging level: trace|debug|info|warn|error|fatal
LOG_LEVEL=info

# Auth tokens
AUTH_JWT_ACCESS_SECRET=access-secret
AUTH_JWT_REFRESH_SECRET=refresh-secret
AUTH_JWT_ACCESS_TTL=900
AUTH_JWT_REFRESH_TTL=604800
EMAIL_VERIFICATION_TTL_MS=86400000
PASSWORD_RESET_TTL_MS=1800000

# Email provider (optional - leave unset to use mock logger)
RESEND_API_KEY=
EMAIL_FROM_ADDRESS="Biometric Core <no-reply@example.com>"
EMAIL_FROM_NAME="Biometric Core"
EMAIL_VERIFICATION_URL=https://app.example.com/verify
PASSWORD_RESET_URL=https://app.example.com/reset-password

# WebAuthn / biometric configuration
# WEBAUTHN_RP_ID:
#   - The "relying party" ID (effective domain) that WebAuthn will bind to.
#   - Must match the domain part of the frontend origin where WebAuthn runs.
#   - For local development with a frontend at http://localhost:3000 use: localhost
WEBAUTHN_RP_ID=localhost

# WEBAUTHN_RP_NAME:
#   - Human-readable application name shown in browser dialogs during registration/login.
#   - This can be any friendly label; it does not need to match the domain.
WEBAUTHN_RP_NAME="Biometric Core (Local)"

# WEBAUTHN_ORIGINS:
#   - Comma-separated list of allowed origins (scheme + host + optional port).
#   - Must exactly match what appears in the browser address bar when calling WebAuthn APIs.
#   - Example for a local frontend on port 3000:
#       http://localhost:3000,https://localhost:3000
WEBAUTHN_ORIGINS=http://localhost:3000,https://localhost:3000

# WEBAUTHN_CHALLENGE_TTL_MS:
#   - Lifetime of WebAuthn challenges in milliseconds.
#   - Defaults to 180000 (3 minutes) if not set.
WEBAUTHN_CHALLENGE_TTL_MS=180000

# WEBAUTHN_SIGNCOUNT_MODE:
#   - Controls how strictly signCount regressions are treated.
#   - "strict" (recommended): reject and revoke credential on regression.
#   - "lenient": log regressions but do not automatically revoke.
WEBAUTHN_SIGNCOUNT_MODE=strict
